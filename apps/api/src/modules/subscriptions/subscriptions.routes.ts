import{Router}from'express';import{admin,auth}from'../../core/auth';import{wrap}from'../../core/errors';import{subscriptionsController as c}from'./subscriptions.controller';export const subscriptionsRouter=Router();subscriptionsRouter.get('/subscription-plans',auth,admin,wrap(c.plans));subscriptionsRouter.post('/subscription-plans',auth,admin,wrap(c.savePlan));subscriptionsRouter.put('/subscription-plans/:id',auth,admin,wrap(c.savePlan));subscriptionsRouter.delete('/subscription-plans/:id',auth,admin,wrap(c.removePlan));subscriptionsRouter.get('/subscriptions',auth,admin,wrap(c.list));subscriptionsRouter.post('/subscriptions',auth,admin,wrap(c.create));subscriptionsRouter.patch('/subscriptions/:id',auth,admin,wrap(c.status));subscriptionsRouter.post('/subscription-payments/:id/pay',auth,admin,wrap(c.pay));subscriptionsRouter.post('/subscription-payments/:id/reopen',auth,admin,wrap(c.reopen));
