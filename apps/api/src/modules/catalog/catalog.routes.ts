import{Router}from'express';import{admin,auth}from'../../core/auth';import{wrap}from'../../core/errors';import{catalogController as c}from'./catalog.controller';export const catalogRouter=Router();catalogRouter.get('/services',auth,wrap(c.services));catalogRouter.post('/services',auth,admin,wrap(c.createService));catalogRouter.put('/services/:id',auth,admin,wrap(c.updateService));catalogRouter.delete('/services/:id',auth,admin,wrap(c.removeService));catalogRouter.get('/clients',auth,admin,wrap(c.clients));catalogRouter.post('/clients',auth,admin,wrap(c.createClient));
